function weaveArrays(arrOne, arrTwo) {
  let candidateChoices = [...arrOne, ...arrTwo];
  function helper(candidateChoices, candidate) {
    if (newCandidate.length === ca)
      for (let i = 0; i < candidateChoices.length; i++) {
        let newCandidate = (candidate += candidateChoices[i]);
      }
  }
}
